# --- SÉCURITÉ : VARIABLES D'ENVIRONNEMENT ---
# Ne JAMAIS commit ces fichiers. Ils contiennent tes secrets (DB, API, etc.)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env*

# --- VERCEL SPECIFIC ---
# Ce dossier contient des métadonnées de liaison de projet (Project ID, Org ID)
.vercel

# --- DÉPENDANCES ---
# Trop lourd et inutile à versionner (se réinstalle avec npm install)
node_modules/
jspm_packages/

# --- BUILDS & OUTPUT ---
# Le résultat de la compilation (Next.js, React, etc.)
.next/
out/
build/
dist/

# --- SYSTÈME & LOGS ---
# Éviter de polluer le repo avec des fichiers spécifiques à ton OS
.DS_Store
Thumbs.db
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- IDE (VS Code / JetBrains) ---
.vscode/
.idea/

# --- FICHIERS TEMPORAIRES VITE ---
# Éviter la pollution avec les fichiers timestamp
vite.config.js.timestamp-*
*.timestamp-*

# --- DOCUMENTATION INTERNE ---
# Dossier de travail Amazon Q (ignoré globalement mais on s'assure)
.amazonq_workfolder/